<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MyStudyHelper.XAML_Pages.LoginPage" Title="Login">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>

            <StackLayout x:Name="StkLoginInfo" Grid.Row="1" Orientation="Vertical" HorizontalOptions="Center" WidthRequest="250">
                <Entry x:Name="txtUsername" Placeholder="Username"/>
                <Entry x:Name="txtPassword" Placeholder="Password"/>
                <StackLayout x:Name="StckLoginBtns" Orientation="Horizontal" HorizontalOptions="Center" Margin="0">
                    <Button x:Name="btnLogin" Text="Login" CornerRadius="20"/>
                    <Button x:Name="btnRegister" Text="Register"/>
                </StackLayout>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>